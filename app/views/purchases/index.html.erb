<h1>Transactions</h1>
<% @purchases.each do |purchase| %>
        <div class="group_container">
          <%= purchase.name%>
           <%= purchase.amount%>
        </div>
    <% end %>
<%= link_to 'New transaction', new_group_purchase_path%>